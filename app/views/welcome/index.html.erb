<h1>Welcome to Wikiful!</h1>
<h2> Here Are Some Recent Articles:</h2>
<p class="lead">Welcome to Wikiful. Most recent articles below:</p>
 <div class="articles">
   <% @recent_articles.each do |article| %>
     <h2>
       <%= link_to article.title, article %>
     </h2>
     <div class="article-info">
    Published <%= article.created_at.strftime('%b %d, %Y') %> |
   
     </div>
     <div class="content">
       <%= truncate(article.content, :length => 100) %><br />
       <%= link_to "Read more", article %>
     </div>
   <% end %>
 </div>

   

